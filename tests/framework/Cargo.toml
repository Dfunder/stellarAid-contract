[package]
name = "integration_framework"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
soroban-sdk = { version = "21.0.0", features = ["testutils"] }
escrow = { path = "../../contracts/escrow", features = ["testutils"] }
platform_config = { path = "../../contracts/platform_config", features = ["testutils"] }
shared = { path = "../../contracts/shared" }